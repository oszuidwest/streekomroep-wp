{% extends "base.twig" %}

{% block content %}
    <div class="px-6 md:px-12 max-w-3xl mx-auto">
        <h1 class="mt-6 mb-8 text-4xl lg:text-5xl font-black font-round">{{ video.name }}</h1>
    </div>

    <div class="w-full mx-auto max-w-960 mt-8">
        {{ embed }}
    </div>
{% endblock %}
