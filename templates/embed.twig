<a class="block bg-gray-200 px-4 py-2 rounded my-5 hover:bg-gray-300" style="text-decoration: none;!important;" href="{{ post.link }}">
    <span class="block font-black font-round text-xs uppercase pt-1">Lees ook:</span>
    <span class="block text-lg font-bold text-blauw">{{ post.title }}</span>
</a>
