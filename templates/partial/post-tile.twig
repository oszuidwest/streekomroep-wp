<a class="block flex sm:flex-col" href="{{ post.link }}">
    <div class="flex-none w-1/3 sm:w-auto">
        <div class="aspect-w-16 aspect-h-9">
            <img class="w-full" loading="lazy" src="{{ post.thumbnail.src|resize(309, 174) }}"
                 srcset="{{ post.thumbnail.src|resize(618, 348) }} 2x"/>
        </div>
    </div>
    <div class="flex-auto pl-2 pr-4 sm:px-0 pt-1">
        {% if region ?? true %}
            {% include 'partial/tag-region.twig' with { region: post.region } %}
        {% endif %}
        <h2 class="sm:text-xl font-semibold my-1">{{ post.title }}</h2>
        <p class="block prose prose-sm {% if dark ?? false %}text-gray-400{% else %}text-gray-600{% endif %}">
            {{ post.date }}
            {% if summary ?? true %}
                <span class="hidden sm:inline px-1">&middot;</span>
                <span class="hidden sm:inline">{{ post.preview.length(20).read_more(false) }}</span>
            {% endif %}
        </p>
    </div>
</a>
