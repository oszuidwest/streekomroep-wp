<a class="block" href="{{ post.link }}">
    {% include 'partial/post-thumbnail.twig' with { post: post } %}
    {% if region ?? true %}
        {% include 'partial/tag-region.twig' with { region: post.region } %}
    {% endif %}
    <h1 class="sm:text-xl font-semibold">{{ post.title }}</h1>
</a>
