{% import 'partial/badge.twig' as badge %}

<div class="flex">
   {% if region %}
       {{ badge.render(region.title, 'groen', (link is not defined or link) ? region.link : null) }}
   {% else %}
       {{ badge.render(options.placeholder_geen_regio, 'groen') }}
   {% endif %}
</div>
