{% import 'partial/badge.twig' as badge %}

<div class="flex">
   {% if region %}
       {% if link is not defined or link %}
           {{ badge.render(region.title, 'brand', region.link) }}
       {% else %}
           {{ badge.render(region.title, 'brand') }}
       {% endif %}
   {% else %}
       {{ badge.render(options.placeholder_geen_regio, 'brand') }}
   {% endif %}
</div>
